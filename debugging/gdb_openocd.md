# GDB and OpenOCD

- **OpenOCD**: Translates GDB commands into JTAG/SWD signaling using a debug probe (like ST-LINK).
- **GDB**: The debugger interface used by the developer to set breakpoints, inspect memory, etc.