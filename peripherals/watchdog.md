# Watchdog Timer (WDT)

Hardware timer that resets the MCU if not periodically reloaded ("kicked" or "fed").

- Protects against software hangs.
- Independent Watchdog (IWDG) runs on a separate internal RC oscillator.