# Stack vs Heap

- **Stack**: Grows downwards. Stores local variables, return addresses. Fast, managed automatically.
- **Heap**: Grows upwards. Dynamic allocation (malloc). Can fragment, dangerous in critical embedded systems.